# auto-generated by grc.converter

id: lora_sdr_fft_demod
label: fft_demod
category: '[LoRa_RX]'

parameters:
-   id: samp_rate
    label: Samp_rate
    dtype: float
-   id: bandwidth
    label: Bandwidth
    dtype: int
-   id: sf
    label: Sf
    dtype: int
-   id: impl_head
    label: Impl_head
    dtype: bool

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ 2**sf }
-   domain: message
    id: CR
-   domain: message
    id: new_frame

outputs:
-   domain: stream
    dtype: int

templates:
    imports: import lora_sdr
    make: lora_sdr.fft_demod(${samp_rate}, ${bandwidth}, ${sf}, ${impl_head})

documentation: "Recover the value of a lora symbol using argmax(DFT(lora_symbol \u2A00\
    \ ref_downchirp)\n    Parameters:\n        Samp_rate: Sampling rate (should be\
    \ equal to the bandwidth in the current implementation)\n        Bandwidth: Bandwidth\n\
    \        sf: spreading factor\n        impl_head: usage of an implicit header\
    \ (explicit will be used otherwise)\n    Input:\n        in: vector of 2^sf complex\
    \ samples\n        CR: coding rate\n        new_frame: indicate beginning of a\
    \ new frame with the value of the reference downchirp (to compensate CFO integer\
    \ part)\n    Output:\n        out: stream of lora symbol decimal values"

file_format: 1
