# auto-generated by grc.converter

id: lora_sdr_modulate
label: Modulate
category: '[LoRa_TX]'

parameters:
-   id: sf
    label: SF
    dtype: int
    default: 'sf'
-   id: samp_rate
    label: Samp_rate
    dtype: int
-   id: bw
    label: Bw
    dtype: int
-   id: sync_words
    label: Sync words
    dtype: int_vector
    hide: part
    default: 0x12

inputs:
-   domain: stream
    dtype: int

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import lora_sdr
    make: lora_sdr.modulate(${sf}, ${samp_rate}, ${bw}, ${sync_words})
    callbacks:
        - set_sf(${sf})
asserts:
- ${ (samp_rate/bw).is_integer()}

documentation: |-
    Add the preamble to the frame and modulate lora symbols according to the input decimal values.
        Parameters:
            sf: spreading factor
            samp_rate: sampling rate (need to be a integer multiple of the bandwidth)
            Bw: LoRa bandwidth
        Input:
            in: stream of decimal symbol values
        Output:
            out: stream of complex valued samples

file_format: 1
