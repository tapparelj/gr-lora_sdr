# auto-generated by grc.converter

id: lora_sdr_deinterleaver
label: Deinterleaver
category: '[LoRa_RX]'

parameters:
-   id: sf
    label: Sf
    dtype: int
-   id: soft_decoding
    label: Soft_Decoding
    dtype: bool
    default: 'soft_decoding'
    options: [False, True]

inputs:
-   domain: stream
    dtype: ${ 'f64' if soft_decoding else 'int'}
    vlen: ${ sf if soft_decoding else 1}

outputs:
-   domain: stream
    dtype: ${ 'f64' if soft_decoding else 'byte'}
    vlen: ${ 8 if soft_decoding else 1}

templates:
    imports: import lora_sdr
    make: lora_sdr.deinterleaver(${sf}, ${soft_decoding})

documentation: |-
    Deinterleave the received codewords.
        Parameters:
            sf: spreading factor
        Input:
            in: stream of received (gray demapped) decimal values
        Output:
            out: stream of deinterleaved codewords (one per byte)

file_format: 1
