# auto-generated by grc.converter

id: lora_sdr_whitening
label: Whitening
category: '[LoRa]/Tx'
flags: [python, cpp]

inputs:
-   domain: message
    id: msg
    optional: true
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: byte

templates:
    imports: import lora_sdr
    make: lora_sdr.whitening()

cpp_templates: 
     includes: ['#include <lora_sdr/whitening.h>'] 
     declarations: 'lora_sdr::whitening::sptr ${id};' 
     make: 'this->${id} = lora_sdr::whitening::make();' 


documentation: |-
    Whiten the payload data with a pseudo random sequence
        Input:
            msg: the payload as a string
        Output:
            out: Stream of whitened payload nibbles

file_format: 1
