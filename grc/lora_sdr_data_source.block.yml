# auto-generated by grc.converter

id: lora_sdr_data_source
label: Data source
category: '[LoRa_TX]'

parameters:
-   id: pay_len
    label: Pay_len
    dtype: int
-   id: n_frames
    label: N_frames
    dtype: int

inputs:
-   domain: message
    id: trigg

outputs:
-   domain: message
    id: msg

templates:
    imports: import gnuradio.lora_sdr as lora_sdr
    make: lora_sdr.data_source(${pay_len}, ${n_frames})

documentation: |-
    Generate a new random payload string every trigger.(Random string formed from characters: a-z, A-Z, 0-9)
        Parameters:
            Pay_len: length of the payload in bytes
            N_frames: number of frames to send
        Input:
            trigg: trigger signal (a message strobe for instance)
        Output:
            msg: payload data as a string

file_format: 1
