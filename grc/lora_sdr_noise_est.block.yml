# auto-generated by grc.converter

id: lora_sdr_noise_est
label: Noise est
category: '[LoRa]/mu'
flags: [python, cpp]

parameters:
-   id: n_samples
    label: N_samples
    dtype: int

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: message
    id: noise_est

templates:
    imports: import lora_sdr
    make: lora_sdr.noise_est(${n_samples})

cpp_templates:
    includes: ['#include <lora_sdr/noise_est.h>']
    declarations: 'lora_sdr::noise_est::sptr ${id};'
    make: 'this->${id} = lora_sdr::noise_est::make(${n_samples});'


file_format: 1
